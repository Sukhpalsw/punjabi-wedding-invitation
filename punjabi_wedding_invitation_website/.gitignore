# Local environment variables — never commit real secrets.
.env
.env.*
!.env.example

# Netlify CLI local state
.netlify/

# npm dependencies (installed fresh by Netlify on deploy from package.json)
node_modules/
